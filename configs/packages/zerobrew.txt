# ZeroBrew-safe minimal userspace packages for Linux/macOS CLI machines.
# Keep this intentionally small; project-specific toolchains should be installed
# per project instead of in the default machine bootstrap.
tmux
uv
